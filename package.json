{
  "name": "andrea-do-val-site",
  "version": "1.0.0",
  "description": "Site Andrea do Val - Psicóloga",
  "scripts": {
    "start": "serve . -l ${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18"
  }
}
